
#Never store password files or anything like it.
.pgpass
.my.cnf
any_password_file

#Never store the .git directory or anything underneath it:
.git/

#Macbook sharts a .DS_Store on file which is for finder, don't commit that.
.DS_Store

#never store the dbext scratch file named 'Result'
Result

#vi stuff, don't commit .swp files, tags files, view files
*.swp
tags
*.view

#Sensible Generic stuff, ignore gigantic video files or data csv files
*.mp4
*.gif
*.csv

#.cast files are halfway ascii files that make images.
*.cast

#Java Essentials, never store class files, ignore jar war ear Package Files
*.class
*.jar
*.war
*.ear

#maven stuff, don't include the directory named target
target

# Mobile Tools for Java (J2ME)
.mtj.tmp/

# virtual machine crash logs, see http://www.java.com/en/download/help/error_hotspot.xml
hs_err_pid*
${0}

